package main

import (
	"fmt"
	"math"
)

func main() {
	fmt.Println("I can retrieve only Exported names "+
		"which start only from Capital letters.\n"+
		"For, example, pi in math is accessed by math.PI: ", math.Pi)
}
